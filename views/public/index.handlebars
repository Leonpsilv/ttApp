<div class="bg-light p-5 rounded-lg m-3">
    <h1 class="display-4">Bem vindo ao TUITER do Leo!</h1>
    <p class="lead"> Esse é um site desenvolvido em Node, Express e MongoDB. Apenas para estudos e prática. seja muito bem vindo e aproveite todas as funcionalidades desenvolvidas! Obs: esse o Elon Musk compraria!</p>
    <hr class="my-4">

    {{#if users}}
    {{else}}
    <p>Para ter acesso a todas as funcionalidades do site, será necessário possuir uma conta.</p>
    <a class="btn btn-primary btn-lg m-1" href="/usuarios/registrar" role="button">Criar conta</a>
    <a class="btn btn-primary btn-lg m-1" href="/usuarios/login" role="button">Fazer login</a>
    <hr>
    {{/if}}

    
</div>
    <hr class="my-4">
<div class="bg-light p-5 rounded-lg m-3">
    {{#each postagens}}
        <div class="card mt-2">
            <div class="card-body">
                <h3>{{usuario.nome}}</h3>
                <small>{{usuario.arroba}}</small>
                <hr class="my-4">
                <p class="mt-3">{{conteudo}}</p>
                <hr class="my-4">
                <small>{{date}}</small>
            </div>
        </div>
    {{else}}
        <h1>Ainda não há postagens no site :/</h1>
    {{/each}}
</div>
